# Desktop Example Assets

Place application resources in this directory when the Vel resource-loading and bundling APIs are available. The current example does not load assets yet; this file keeps the resource boundary visible without pretending that bundling is implemented.
